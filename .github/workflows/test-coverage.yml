# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: test-coverage

jobs:
  test-coverage:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4                    # v3 → v4

      - uses: r-lib/actions/setup-r@v2
          with:
            use-public-rspm: true

      - uses: r-lib/actions/setup-pandoc@v2          # v1 → v2

      - uses: r-lib/actions/setup-r-dependencies@v2  # replaces manual cache + install steps
        with:
          extra-packages: any::covr
          needs: coverage

      - name: Test coverage
        run: covr::codecov()
        shell: Rscript {0}